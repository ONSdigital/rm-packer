{
  "variables": {
    "aws_access_key": null,
    "aws_secret_key": null,
    "region": null,
    "subnet_id": null,
    "vpc_id": null
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "{{user `region`}}",
    "source_ami": "ami-9398d3e0",
    "instance_type": "t2.large",
    "ssh_username": "ec2-user",
    "ami_name": "respondent-home-v9.31.0 ({{timestamp}})",
    "subnet_id": "{{user `subnet_id`}}",
    "vpc_id": "{{user `vpc_id`}}"
  }]
}
